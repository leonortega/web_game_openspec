## Implementation Tasks

- [x] Add a main menu entry flow with `Start`, master volume, enemy pressure, rules view, and difficulty selection.
- [x] Add persistent run settings so the selected menu options are applied when gameplay starts.
- [x] Implement authored interactive reward blocks for coin rewards and the four fixed powers.
- [x] Move power acquisition out of stage-specific unlocks and into the new block reward system.
- [x] Persist active powers across stage transitions and clear them when the player is hurt or dies.
- [x] Add per-level coin tracking and restore full energy when the player collects every coin.
- [x] Update HUD and transition text so the player can read active powers, coin progress, and selected settings.
- [x] Verify stage authoring and gameplay flow still support the existing checkpoint and completion rules.
